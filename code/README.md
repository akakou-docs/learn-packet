# 実行の仕方

## 環境
Linux

## ビルド
```sh
cd client && make                                                                                                                         
cd ../server && make
```

## 実行
`client/bin/`
`server/bin/`

にそれぞれ実行ファイルができていると思います。
